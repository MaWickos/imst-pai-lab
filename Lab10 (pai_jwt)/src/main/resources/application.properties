# Secret key wykorzystywany przez algorytm haszuj?cy
# Dodawany przez JWT do kompinacji z nag?owkiem i ?adunkiem (pyload) z danymi
jwt.secret = Programowanie_ai2021

# Spring DATASOURCE (konfiguracja i w?a?ciwo?ci ?ród?a danych)
spring.datasource.url=jdbc:mysql://localhost:3306/notesdb?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=
spring.datasource.platform=mysql
spring.datasource.initialization-mode=always

# W?a?ciwo?ci dla Hibernate - dialekt
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect

# Ustawienia dla Hibernate dla operacji DDL (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=create-drop


#
spring.main.allow-circular-references=true
